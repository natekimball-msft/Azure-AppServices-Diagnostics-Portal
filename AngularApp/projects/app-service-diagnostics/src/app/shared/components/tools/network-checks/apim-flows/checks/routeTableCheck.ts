import { VirtualNetworkType } from '../contracts/APIMService';

export function routeTableCheck(networkType: VirtualNetworkType) {
    if (networkType != VirtualNetworkType.NONE) {
    } else {
    }
}
